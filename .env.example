# Server port (default: 3001)
PORT=3001

# CORS origin for production (default: * allows all origins)
# CORS_ORIGIN=https://yourdomain.com

# PlateStack integration (optional — omit to disable)
# PLATESTACK_DB_PATH=/path/to/platestack.db

# Node environment
NODE_ENV=production
